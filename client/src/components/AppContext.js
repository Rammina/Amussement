import React from "react";

// export const ElementsContext = React.createContext(null);
export const NavContext = React.createContext(null);
// export const GoogleAuthContext = React.createContext(null);
